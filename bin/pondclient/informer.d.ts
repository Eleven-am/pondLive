import { Channel } from "./channel";
import { DomWatcher } from "./domWatcher";
export declare const informer: (channel: Channel, watcher: DomWatcher) => void;
export declare const manageRoutes: (channel: Channel) => void;
